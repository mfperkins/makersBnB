html

head

body
  h1= h1

ul

  each val in spaces

    li(id='title' )= val.dataValues.title + ' '
    li(id='description')= val.dataValues.description + ' '
    li(id='price')= val.dataValues.price + ' '
    li(id='availability')= val.dataValues.availability + ' '

    a(href='http://localhost:3000/spaces/' + val.dataValues.id) Click here to edit this space!

    br
